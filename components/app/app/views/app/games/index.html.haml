%p#notice= notice
%h1 Listing Games
%table
  %thead
    %tr
      %th Date
      %th Location
      %th First team
      %th Second team
      %th Winning team
      %th First team score
      %th Second team score
      %th{:colspan => "3"}
  %tbody
    - @games.each do |game|
      %tr
        %td= game.date
        %td= game.location
        %td= game.first_team_id
        %td= game.second_team_id
        %td= game.winning_team
        %td= game.first_team_score
        %td= game.second_team_score
        %td= link_to 'Show', game
        %td= link_to 'Edit', edit_game_path(game)
        %td= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Game', new_game_path
